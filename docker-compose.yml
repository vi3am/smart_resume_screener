services:
  db:
    image: postgres:16
    container_name: resume_screener_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: visal_1505
      POSTGRES_DB: resume_screener
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: